Sources for the plugin `plugin.applovinMax.GoogleAdManager`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.GoogleAdManager" = {
            publisherId = "com.solar2d",
        },
    },
}
```
